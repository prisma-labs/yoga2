{
  "name": "yoga2",
  "private": true,
  "scripts": {
    "bootstrap": "npm install && lerna bootstrap",
    "clean": "lerna clean -y && npm run clean:packages && npm run clean:root",
    "clean:packages": "lerna exec -- rm -rf node_modules yarn.lock package-lock.json dist",
    "clean:root": "rm -rf node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm run bootstrap",
    "build": "lerna run build",
    "lint": "lerna exec -- npm run lint"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "3.13.0"
  }
}
